name: test
channels:

  - conda-forge

  - defaults
dependencies:
    # Base depends
  - python
  - pip

    # Testing
  - pytest
  - pytest-cov
  - codecov
  - pip:
    - sphinx-toolbox
    - git+https://github.com/OpenFreeEnergy/gufe@main
